[project]
name = 'Personal-Data-Liberation-Front'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.data-export-automation]
path = 'contracts/data-export-automation.clar'
clarity_version = 3
epoch = '3.2'

[contracts.privacy-dashboard]
path = 'contracts/privacy-dashboard.clar'
clarity_version = 3
epoch = 'latest'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
